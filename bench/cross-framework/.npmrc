# Isolated benchmark — install scripts disabled by default.
ignore-scripts=true
# This package is not on the secure-by-default 24h cooldown because it is a
# throwaway dev-only harness that never ships. Do not copy this .npmrc into
# any package that publishes to npm.
