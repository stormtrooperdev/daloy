node_modules/
*/node_modules/
dist/
dist-coverage/
coverage/
.DS_Store
*.log
.env
.env.*
!.env.example

# Build artifacts from `pnpm gen`
generated/

# pnpm
.pnpm-store/

# npm shrinkwrap — DaloyJS publishes via pnpm with `frozen-lockfile=true`
# and `verify-store-integrity=true`; an `npm-shrinkwrap.json` checked in
# here would silently override that posture for anyone installing
# `@daloyjs/core` via plain `npm install`. Enforced at CI by
# `pnpm verify:no-shrinkwrap` (Socket npm-shrinkwrap gate).
npm-shrinkwrap.json
package-lock.json


otherdocs


test_output.txt

honojs-large-fakerestapi
daloyjs-large-fakerestapi
coverage_output.txt
.vercel

# Bench runtime artifacts only — keep bench/*.ts and bench/cross-framework/ tracked.
bench/**/node_modules/
bench/**/results.json
bench/cross-framework/pnpm-lock.yaml
bench/cross-framework/package-lock.json